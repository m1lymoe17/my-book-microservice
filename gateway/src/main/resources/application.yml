server:
  port: 8086

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: book
          uri: lb://book
          predicates:
            - Path=/api/book/**
        - id: user
          uri: lb://user
          predicates:
            - Path=/api/user/**